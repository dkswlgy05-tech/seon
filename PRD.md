# 미래사업팀 주간업무 대시보드 — MVP PRD

> 1-page MVP · v0.1 · 2026-05-28 · Owner: 미래사업팀

## 1. 문제와 목표

미래사업팀은 매주 수행/발주예상 프로젝트 현황을 HWPX 문서에 수기로 정리하고 있어 ① 데이터 누락·형식 오류, ② 버전 충돌, ③ 포트폴리오 한눈 파악 불가의 3가지 반복 문제를 겪고 있다. 본 MVP는 **웹에서 인라인으로 데이터를 입력/수정**하고, **단일 페이지에서 전체 현황을 즉시 확인**하며, **기존 HWPX 양식 그대로 자동 다운로드**할 수 있는 대시보드를 제공한다.

**성공 지표 (4주 후)**
- 주간 보고서 작성 시간: 60분 → 10분 이내
- 한컴 오피스 수기 수정 횟수: 주 0회 (HWPX는 다운로드만)
- 팀원 4명 이상이 로그인 없이 데이터 갱신에 참여

## 2. 사용자 & 핵심 시나리오

| 사용자 | 시나리오 |
|---|---|
| 단장 / PM | 본인 담당 프로젝트의 제출일·개찰일·내용을 인라인으로 갱신 |
| 팀 리더 | 상단 캘린더로 이번 주 개찰 임박 건 확인, 누락 입력 보완 |
| 보고 담당 | 매주 월요일 09시에 "HWPX 다운로드" 클릭 → 기존 양식 그대로 공유 |

## 3. 화면 구성 (Single Page)

```
┌─────────────────────────────────────────────────────────────┐
│ 헤더: 미래사업팀 주간업무 · [주차 선택 ▾] · [HWPX 다운로드]    │
├─────────────────────────────────────────────────────────────┤
│ ① 월별 개찰 일정 캘린더 (FullCalendar 월뷰, 칩으로 용역명 표시) │
├─────────────────────────────────────────────────────────────┤
│ ② 수행 Project — 인라인 표 편집                             │
│    [+ 행 추가]  탭: 개찰 | 진행중                           │
│    구분 · 연번 · 용역명 · 단장 · 제출일 · 발표/면접 · 개찰일 · │
│    용역비(억) · 내용                                         │
├─────────────────────────────────────────────────────────────┤
│ ③ 발주예상 Project — 인라인 표                              │
│    연번 · Project · 발주청 · 단장 · 사업비 · 발주(월) · 용역비 · 내용 │
├─────────────────────────────────────────────────────────────┤
│ ④ 교육참가자(OSG팀) — 분야 칩 에디터                         │
│    [책임 기술자] [건축] [토목] [안전] [기계] 각 그룹별 칩 추가/삭제 │
├─────────────────────────────────────────────────────────────┤
│ ⑤ 기타 — 자유 텍스트 (마크다운 lite)                         │
└─────────────────────────────────────────────────────────────┘
```

- 디자인 톤: Linear/Notion 스타일 — 8px 그리드, 뉴트럴 그레이 + 단일 액센트(블루), Inter/Pretendard, 헤어라인 보더, hover 시 행 강조.
- 인라인 편집: 셀 클릭 → 즉시 input. 날짜는 date picker, 단장은 자동완성, 구분/탭은 segmented control. blur 시 Supabase auto-save.

## 4. 데이터 모델 (Supabase)

```
weeks            (id, week_label "2026.5.20.", created_at)
projects         (id, week_id FK, kind enum[수행|발주예상], status enum[개찰|진행중|null],
                  seq, name, client, leader, submit_date, interview_date,
                  bid_date, budget, fee, note)
education_members(id, week_id FK, role enum[책임|건축|토목|안전|기계], name, org)
etc_notes        (id, week_id FK, body text)
```
- "주차 선택 ▾"으로 `week_id` 컨텍스트 전환. 신규 주차 생성 시 직전 주차 데이터를 복제(carry-over)하고 개찰 완료 항목은 자동으로 status=null 처리.

## 5. HWPX 자동 생성

- **전략**: `practice.hwpx`를 **템플릿**으로 두고, `Contents/section0.xml`의 표 셀(`<hp:tc>` 내부 `<hp:t>`)을 placeholder 토큰(`{{P1.name}}` 등)으로 치환한 마스터를 보관 → 다운로드 시 JSZip으로 압축 해제 → 토큰 치환 → 재압축.
- **유지 항목**: rowSpan/colSpan, 번호 셀 배경색, 항번 자동 재계산은 기존 [hwpx-section-mover] 스킬 로직과 동일한 규칙으로 처리.
- **범위**: 1) 수행 / 2) 발주예상 / 3) 교육참가자 / 4) 기타 — **4섹션 전체** 자동 생성.
- 다운로드 파일명: `미래사업팀_주간업무_{week_label}.hwpx`.

## 6. 기술 스택 & 아키텍처

- **Frontend**: Next.js 15 (App Router) · React 19 · TypeScript · Tailwind + shadcn/ui · TanStack Table(인라인 편집) · FullCalendar(캘린더) · zod(검증)
- **Backend**: Supabase Postgres (RLS off — MVP 무로그인) · Supabase JS SDK로 직접 호출
- **HWPX 빌더**: Next.js Route Handler `/api/hwpx`에서 `jszip` + `fast-xml-parser`로 템플릿 치환 후 Blob 응답
- **호스팅**: Vercel

## 7. MVP 범위 (In / Out)

**In** — 단일 페이지, 인라인 표 편집, 월별 개찰 캘린더, 4섹션 HWPX 자동 다운로드, 주차 단위 데이터 관리, 자동 저장
**Out (차기)** — 로그인/권한, 코멘트/멘션, 변경 이력, 차트 위젯 확장(단장별/금액별), 모바일 최적화, Slack/이메일 알림

## 8. 마일스톤 (4주)

| 주차 | 산출물 |
|---|---|
| W1 | Supabase 스키마 + Next.js 프로젝트 셋업, 표 1) 인라인 편집 동작 |
| W2 | 표 2)·3)·4) + 주차 전환 + 월별 캘린더 |
| W3 | HWPX 템플릿 치환 엔진 + 4섹션 출력, 한컴에서 열람 검증 |
| W4 | 톤 정리(Linear 스타일), 팀 4명 파일럿, 피드백 반영 |

## 9. 열린 질문

1. 주차 전환 시 carry-over 규칙 — 개찰 완료 자동 분류 기준이 "개찰일 < 오늘"이면 충분한가?
2. 용역비 단위는 "억원" 고정인가, 자유 입력 허용인가?
3. 단장 명단(추영욱·김종완 등)은 마스터로 관리할 것인가, 자유 입력인가? — 자동완성 정확도에 영향.
